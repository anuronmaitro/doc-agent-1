

## 1. Introduction

The \(\beta\)-function \(\beta\) is a function of the form

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.1)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.2)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.3)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.4)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.5)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.6)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.7)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.8)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.9)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.10)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.11)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.12)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.13)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.14)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{\beta}\] (1.15)

where \(\beta(x)\) is the real part of the function \(\beta(x)\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1