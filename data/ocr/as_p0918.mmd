

## Appendix A Numerical analysis

### A.1.1 The \(\beta\)-function

The \(\beta\)-function is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (1)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (2)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (3)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (4)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (5)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (6)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (7)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (8)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (9)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (10)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (11)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (12)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (13)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (14)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (15)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as

\[\beta(x)=\frac{1}{\beta(x)}\left(\frac{1}{\beta(x)}\right)^{-1}\] (16)

where \(\beta(x)\) is the real part of the function \(x\). The function \(\beta(x)\) is defined as